interface:
  display_name: "Codex Orchestration"
  short_description: "Optional plan reviewer plus efficient executors"
  default_prompt: "Use $codex-orchestration to choose an executor and optional advisor while the current task model stays in charge."

policy:
  allow_implicit_invocation: false
